name: Release to PyPI

on:
  release:
    types: [published]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package
        run: pip install -e ".[dev]" 2>/dev/null || pip install -e . && pip install pytest

      - name: Run tests
        run: python -m pytest tests/ -v --tb=short

  publish:
    runs-on: ubuntu-latest
    needs: test
    permissions:
      id-token: write
    environment:
      name: pypi
      url: https://pypi.org/project/mcp-testkit/
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Verify version is not already published
        run: |
          PKG_VERSION=$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])")
          echo "Releasing version: $PKG_VERSION"
          if curl -sf "https://pypi.org/pypi/mcp-testkit/$PKG_VERSION/json" > /dev/null; then
            echo "::error::mcp-testkit $PKG_VERSION is already published on PyPI. Bump the version in pyproject.toml (and mcp_test_server/__init__.py) before cutting a release."
            exit 1
          fi

      - name: Install build tools
        run: pip install build

      - name: Build sdist and wheel
        run: python -m build

      - name: Verify wheel
        run: |
          pip install dist/*.whl
          mcp-testkit --help
          python -c "from mcp_test_server import __version__; print(f'Version: {__version__}')"

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true
